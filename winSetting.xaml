<Window x:Class="VPet.Plugin.MathGenius.winSetting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ll="clr-namespace:LinePutScript.Localization.WPF;assembly=LinePutScript.Localization.WPF"
        mc:Ignorable="d"
        Title="{ll:Str 数学天才 设置}" MinHeight="100" MinWidth="150" WindowStartupLocation="CenterScreen" 
        Background="{DynamicResource DARKPrimary}" Foreground="{DynamicResource DARKPrimaryText}"
        SizeToContent="WidthAndHeight">
    <Window.Resources>
        <ResourceDictionary Source="/VPet-Simulator.Windows.Interface;component/ResourceStyle.xaml" />
    </Window.Resources>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
        <CheckBox x:Name="CbHook" Content="{ll:Str 启用}" Margin="0,0,0,12" Foreground="{DynamicResource DARKPrimaryText}"/>
        <CheckBox x:Name="CbAutoType" Content="{ll:Str 萝莉斯帮你写}" Margin="0,0,0,12" Foreground="{DynamicResource DARKPrimaryText}"/>
    </StackPanel>
</Window>
